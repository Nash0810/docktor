# Your updated Dockerfile.stress-test
FROM debian:latest
# This RUN command is missing the cleanup and should trigger PERF002
RUN apt-get update && apt-get install -y curl \
    && rm -rf /var/lib/apt/lists/*
# This RUN is fine on its own for now
RUN useradd -m myappuser
USER myappuser
WORKDIR /home/myappuser
CMD ["echo", "Hello from the container"]